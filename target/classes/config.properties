# Credenciales reales de conexion a la base de datos MySQL en Aiven.
# OJO: este archivo esta en .gitignore a proposito -> NUNCA se sube a GitHub.
# Si por accidente llegas a subirlo, cambia la contrasena de inmediato en Aiven.

# La URL apunta a "rapidexpress_db" (no a "defaultdb") porque el script
# 1_schema_ddl.sql crea esa base de datos dentro del mismo servicio MySQL.
db.url=jdbc:mysql://mysql-3ebb1bdd-katherinecano020-f4b2.j.aivencloud.com:21896/rapidexpress_db?sslMode=REQUIRED

db.user=avnadmin

# Pega aqui tu password real (la que aparece "revelada" en Aiven, no los asteriscos).
db.password=PON_AQUI_TU_PASSWORD_REAL
